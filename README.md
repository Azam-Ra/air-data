# Air Data Utilities

This repository is the shared data hub for the air pollution related projects. It also contains utility scripts for parsing CSV files with data.

## Contents
- `data/` — datasets and raw inputs
- `scripts/` — utility scripts for CSV parsing and related data processing
